import React from 'react';
import { expect } from 'chai';

import HomePage from './HomePage';


describe('<ManageResourcesPage />', () => {
    it('should pass', () => {
        expect(true).to.equal(true);
    });
});
